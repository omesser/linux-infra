---
kind: PrismaPandaDevDeployment
metadata:
  name: panda-sw-staging
  timezone: timezone


machines:
  panda-sw-staging:
    system_type: 'dev'
    
    # Connectivity to the bare-metal (proxmox host)
    ansible_host: "10.50.0.141"
    ansible_user: "root"
    ansible_password: "Pa$$w0rd!"


    proxmox_host_ip: "10.50.0.141"
    proxmox_user: "root@pam"
    proxmox_user_password: "Pa$$w0rd!"
    proxmox_node: "vm1"

    template_vmid: 9000
    
    net_bridge:
      name: "vmbrStaging"
      netmask: "255.255.255.0"
      cidr: "40.0.0.1/24"

    # vms configuration #
    vms:
      yuval-test-staging:
        name: "yuval-test-staging"
        type: workernode
        username: "prisma"
        password: "prisma"
        ssh_key_file: "/root/.ssh/id_rsa.pub"
        ip: "40.0.0.2"
        dhcp: true  # NEEDS TO BE ADDED AND CONFIGURABLE BUT FOR NOW THIS IS WHAT IT IS
        sockets: 2
        cores: 20
        memory: 4096
        gpu: true
        mounts:
          src: "/mnt/data-to-share"
          dest: "/mnt/idan-is-loser"
          tag: "idan-share"

